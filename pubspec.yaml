name: watchtheflix
description: A cross-platform IPTV Flutter application with M3U playlist support.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5

  # Networking
  dio: ^5.4.0
  http: ^1.2.0

  # Video Player
  video_player: ^2.8.2
  chewie: ^1.7.4

  # Storage
  shared_preferences: ^2.5.4
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2

  # UI Components
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  flutter_svg: ^2.0.9
  carousel_slider: ^5.1.1
  google_fonts: ^6.1.0

  # XML Parsing
  xml: ^6.5.0

  # Utils
  get_it: ^9.2.0
  injectable: ^2.7.1+2
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  intl: ^0.20.2
  collection: ^1.18.0
  flutter_staggered_grid_view: ^0.7.0
  url_launcher: ^6.2.4
  logger: ^2.0.2+1
  connectivity_plus: ^7.0.0

  # Icons
  cupertino_icons: ^1.0.6
  material_design_icons_flutter: ^7.0.7296

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  injectable_generator: ^2.4.1
  hive_generator: ^2.0.1
  bloc_test: ^10.0.0
  mocktail: ^1.0.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/mock/